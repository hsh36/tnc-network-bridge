{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "composite": true,
    "rootDir": ".",
    "outDir": "../../dist/shared",
    "tsBuildInfoFile": "../../dist/.tsbuildinfo/shared.tsbuildinfo",

    "types": [],
    "lib": ["ES2022"]
  },
  "//types": "`types` is deliberately empty. `shared` is consumed by the browser bundle, so it must not be able to reach Node globals or any backend type. This is the compiler-enforced half of T2's 'zero backend imports' criterion.",
  "include": ["**/*.ts"],
  "exclude": ["**/*.test.ts", "**/__tests__/**"]
}
